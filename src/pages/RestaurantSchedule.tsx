
import RestaurantScheduleContent from '@/components/restaurant/RestaurantScheduleContent';

const RestaurantSchedule = () => {
  return <RestaurantScheduleContent />;
};

export default RestaurantSchedule;
